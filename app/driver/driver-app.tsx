"use client";
import { FormEvent,useEffect,useState } from "react";

type R=Record<string,string|number|null>;type Driver={id:number;name:string};type DriverData={driver:Driver;attendance:R|null;schedules:R[]};
const locate=()=>new Promise<{latitude:number|null;longitude:number|null}>(resolve=>{if(!navigator.geolocation)return resolve({latitude:null,longitude:null});navigator.geolocation.getCurrentPosition(p=>resolve({latitude:p.coords.latitude,longitude:p.coords.longitude}),()=>resolve({latitude:null,longitude:null}),{enableHighAccuracy:true,timeout:8000});});
export default function DriverMobileApp(){const [drivers,setDrivers]=useState<Driver[]>([]),[data,setData]=useState<DriverData|null>(null),[loading,setLoading]=useState(true),[busy,setBusy]=useState(false),[message,setMessage]=useState("");
 const refresh=async()=>{const auth=await fetch('/api/driver/auth').then(r=>r.json());setDrivers(auth.drivers||[]);if(auth.driver){const d=await fetch('/api/driver').then(r=>r.json());setData(d)}else setData(null);setLoading(false)};useEffect(()=>{refresh()},[]);
 async function login(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget),r=await fetch('/api/driver/auth',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f))}),j=await r.json();if(!r.ok)setMessage(j.error);else await refresh();setBusy(false)}
 async function act(action:string,scheduleId?:number,extra:R={},photos?:{mileage:File|null;fuel:File|null}){setBusy(true);setMessage('');if(action==='complete'&&scheduleId&&photos){for(const [photoType,file] of Object.entries(photos)){if(!file)continue;const form=new FormData();form.append('photo',file);form.append('scheduleId',String(scheduleId));form.append('photoType',photoType);const upload=await fetch('/api/driver/photos',{method:'POST',body:form}),result=await upload.json();if(!upload.ok){setMessage(result.error||'Photo upload failed');setBusy(false);return}}}const position=await locate();const r=await fetch('/api/driver',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action,scheduleId,...extra,...position})}),j=await r.json();setMessage(r.ok?'Update saved':j.error);if(r.ok)await refresh();setBusy(false)}
 if(loading)return <div className="driver-app splash">Loading Fleetway Driver…</div>;
 if(!data)return <main className="driver-app login-screen"><div className="driver-logo">F</div><p className="eyebrow">FLEETWAY DRIVER</p><h1>Ready for the road?</h1><p className="driver-muted">Sign in to view your assigned route and report today’s activity.</p><form className="driver-login" onSubmit={login}><label>Driver<select name="driverId" required><option value="">Select your name</option>{drivers.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select></label><label>PIN<input name="pin" type="password" inputMode="numeric" minLength={4} maxLength={8} required placeholder="4–8 digit PIN"/></label>{message&&<div className="driver-message error">{message}</div>}<button disabled={busy}>{busy?'Signing in…':'Sign in'}</button><small>Demo drivers: use initial PIN <b>1234</b>.</small></form></main>;
 const checkedIn=!!data.attendance&&!data.attendance.check_out_at;return <main className="driver-app"><header className="driver-header"><div><p className="eyebrow">FLEETWAY DRIVER</p><h1>Hello, {data.driver.name.split(' ')[0]}</h1></div><button onClick={async()=>{await fetch('/api/driver/auth',{method:'DELETE'});setData(null)}}>Sign out</button></header><section className={`attendance-card ${checkedIn?'checked':''}`}><div><span className="status-dot"/><p>{checkedIn?'You are checked in':'Attendance not started'}</p><small>{checkedIn?`Since ${new Date(String(data.attendance?.check_in_at)).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}`:'Location is captured with attendance'}</small></div><button disabled={busy} onClick={()=>act(checkedIn?'checkOut':'checkIn')}>{checkedIn?'Check out':'Check in'}</button></section>{message&&<div className="driver-message">{message}</div>}<section className="driver-section"><div className="driver-section-title"><h2>My routes</h2><span>{data.schedules.length} assigned</span></div>{data.schedules.map(s=><TripCard key={s.id} trip={s} busy={busy} act={act}/>)}</section><nav className="driver-bottom"><span className="selected">⌂<small>Today</small></span><span>□<small>History</small></span><span>◎<small>Profile</small></span></nav></main>}
function TripCard({trip,busy,act}:{trip:R;busy:boolean;act:(a:string,id?:number,e?:R,p?:{mileage:File|null;fuel:File|null})=>void}){const [odo,setOdo]=useState(''),[fuel,setFuel]=useState(''),[cost,setCost]=useState(''),[notes,setNotes]=useState(''),[mileagePhoto,setMileagePhoto]=useState<File|null>(null),[fuelPhoto,setFuelPhoto]=useState<File|null>(null);const complete=!!trip.completed_at;let step='Start loading';if(trip.loading_started_at)step='Depart';if(trip.departed_at)step='Start unloading';if(trip.unloading_started_at)step='Complete delivery';const action=trip.unloading_started_at?'complete':trip.departed_at?'startUnloading':trip.loading_started_at?'depart':'startLoading';return <article className="trip-card"><div className="trip-top"><div><b>{trip.route_code}</b><small>{trip.date} · {trip.time}</small></div><em className={complete?'done':''}>{complete?'Completed':trip.status}</em></div><div className="route-line"><span>{trip.origin}</span><i/><span>{trip.destination}</span></div><div className="trip-meta"><span>▣ {trip.plate}</span><span>◫ {trip.cargo_summary||'No goods listed'}</span></div>{trip.last_location_at&&<small className="live-location">● Location updated {new Date(String(trip.last_location_at)).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</small>}{!complete&&<div className="trip-action">{action==='startLoading'&&<label>Starting mileage<input value={odo} onChange={e=>setOdo(e.target.value)} inputMode="numeric" type="number" placeholder="Odometer km"/></label>}{action==='complete'&&<><label>Final mileage<input value={odo} onChange={e=>setOdo(e.target.value)} type="number" inputMode="numeric" placeholder="Odometer km"/></label><div className="driver-fields"><label>Fuel used (L)<input value={fuel} onChange={e=>setFuel(e.target.value)} type="number" inputMode="decimal" placeholder="Litres"/></label><label>Fuel cost<input value={cost} onChange={e=>setCost(e.target.value)} type="number" inputMode="decimal" placeholder="Amount"/></label></div><div className="photo-upload-grid"><label className="photo-upload">Mileage photo<span>{mileagePhoto?'✓ '+mileagePhoto.name:'Take or choose odometer photo'}</span><input type="file" accept="image/jpeg,image/png,image/webp" capture="environment" onChange={e=>setMileagePhoto(e.target.files?.[0]||null)}/></label><label className="photo-upload">Fuel photo<span>{fuelPhoto?'✓ '+fuelPhoto.name:'Receipt, pump or fuel gauge'}</span><input type="file" accept="image/jpeg,image/png,image/webp" capture="environment" onChange={e=>setFuelPhoto(e.target.files?.[0]||null)}/></label></div><small className="photo-help">Optional · JPG, PNG or WebP · maximum 8 MB each</small><label>Notes<textarea value={notes} onChange={e=>setNotes(e.target.value)} placeholder="Delivery or fuel notes"/></label></>}<button disabled={busy||(action==='startLoading'&&!odo)||(action==='complete'&&!odo)} onClick={()=>act(action,Number(trip.id),{odometerKm:odo,fuelLitres:fuel,fuelCost:cost,notes},{mileage:mileagePhoto,fuel:fuelPhoto})}>{busy?'Saving…':step}</button></div>}{complete&&<div className="trip-result"><span>Distance <b>{Number(trip.end_odometer_km)-Number(trip.start_odometer_km)} km</b></span><span>Fuel <b>{trip.fuel_litres||0} L</b></span></div>}</article>}
